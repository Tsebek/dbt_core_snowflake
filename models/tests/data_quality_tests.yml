version: 2

models:
  - name: tr_cpi_index
    columns:
      - name: seasonal_adjustment
        tests:
          - accepted_values:
              values: ['Y', 'N']
      - name: "\"Date\""
        tests:
          - not_null